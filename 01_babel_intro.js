// What is Babel?
// Babel 是什么？

// Babel is a JavaScript compiler that can convert newer versions (like ES6+) of code into older versions (like ES5), allowing code to run in older browsers or environments.
// Babel 是一个 JavaScript 编译器，可以把新版本（如 ES6+）的代码转换为旧版本（如 ES5），让代码能在老旧浏览器或环境中运行。

// Example: ES6 arrow function
// 简单例子：ES6 箭头函数

const add = (a, b) => a + b;
console.log(add(2, 3)); // Output: 5 // 输出：5

// You can use Babel to transpile this file and see how Babel works.
// 你可以用 Babel 转换本文件，体验 Babel 的作用。
