"use strict";

// This is the code transpiled by Babel, compatible with legacy environments
// 这是 Babel 转译后的代码，兼容老旧环境

var greet = function greet(name) {
  // Return a greeting string
  // 返回问候字符串
  return "Hello, ".concat(name, "!");
};

// Output greeting to console
// 在控制台输出问候语
console.log(greet('Babel')); 
